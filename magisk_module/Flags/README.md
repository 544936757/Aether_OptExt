# Flags - 功能开关
# 创建以下空文件来启用对应的功能:
#
# update_config   - 强制用模块配置覆盖用户自定义配置（默认启用）
# modtify_config  - 自动适配不同核心架构（默认启用）
# keep_custom_rule - 增量更新保留用户自定义规则
# delete_game_config - 删除自带游戏配置
# keep_Asoulopt   - 删除与 Asoulopt 冲突的规则
